[project]
name = "snacker-tracker-scanner-rpi-pico"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.1.1",
]

[dependency-groups]
dev = [
    "pytest-mock",
    "requests",
    "requests-mock",
]
